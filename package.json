{
  "name": "mc-website",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo Building static site && mkdocs build && cp -r public/* docs/",
    "start": "serve ./docs -l 5000"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
