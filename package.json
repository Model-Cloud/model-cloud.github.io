{
  "name": "mc-website",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "serve -s . -l 5000",
    "build": "echo Building static site && npm run docs:build",
    "start": "serve ./public -l 5000",
    "docs:build": "mkdocs build"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
